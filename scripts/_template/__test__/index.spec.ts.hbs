import { mount } from '@vue/test-utils'
import {{properCase name}} from '../index.vue'

describe('{{properCase name}}.vue', () => {
  test('render', () => {
    const wrapper = mount({{properCase name}}, {
      props: {
        name: 'scan',
      },
    })
    expect(wrapper.classes()).toContain('md-{{dashCase name}}')
  })
})
